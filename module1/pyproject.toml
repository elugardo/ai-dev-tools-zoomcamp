[project]
name = "city-journal"
version = "0.1.0"
description = "A personal place journal for a city I just moved to"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1.1",
    "rapidfuzz>=3.14.6",
]
