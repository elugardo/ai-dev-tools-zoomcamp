# Where the real FastAPI backend will live (Phase 3). Unused while the mock is active.
VITE_API_BASE_URL=http://localhost:9127/api

# "mock" runs the whole app against the in-browser mock service. It is the only
# implementation today, so this is also the default when unset.
VITE_SERVICE_MODE=mock
