# Copy to .env.local to override. Both values below are the defaults.

# Where the FastAPI backend lives.
VITE_API_BASE_URL=http://localhost:9127/api

# "http" talks to the backend above. "mock" runs the whole app on the in-browser
# mock services layer, with no backend needed.
VITE_SERVICE_MODE=http
